"""A minimal, fully local multi-agent pipeline.

The pipeline treats auditability and human oversight as first-class features.
It is a reference pattern for building controllable agents, not a production
system. See the README for the architecture and design decisions.
"""

__version__ = "0.1.0"
